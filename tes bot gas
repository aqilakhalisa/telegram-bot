tg.kirimPesan('1051577909','tes');
var raw_text = JSON.stringify(update);
for(is in update){
 if(/(edited_)*(message|channel_post)/g.exec(is)){
   var msg = update[is];
 }
}
if(/^\/start/g.exec(raw_text)){
 tg.sendMsg(msg, 'hai bot ini di buat menggunakan gas dan github');
}

